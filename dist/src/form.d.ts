import { RenderableProps } from 'preact';
import { SignalFormProps } from './types';
export declare function SignalForm<T extends object>(p: RenderableProps<SignalFormProps<T>>): import("preact").JSX.Element;
